# SP26 Datathon

## Setup
```bash
cd work
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Dependencies
- pandas
- matplotlib
- numpy
- seaborn
- scipy

## Data

- `ai_survey_normalized_clustering_data.csv` — normalized AI survey data for clustering
- `ai_survey_results_2024_n=930.csv` — AI survey results (2024, n=930)

## Structure
- `work/main.ipynb` — main analysis notebook
- `work/requirements.txt` — Python dependencies

